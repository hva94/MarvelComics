<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/ivVariant"
        android:layout_width="85dp"
        android:layout_height="128dp"
        android:layout_marginEnd="13dp"
        android:background="@android:color/darker_gray"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:scaleType="centerCrop"
        app:layout_constraintDimensionRatio="72:72"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearance="@style/Theme.AndroidChallenge.PhotoGrid" />

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/common_padding_min"
        android:maxLines="2"
        app:layout_constraintEnd_toEndOf="@+id/ivVariant"
        app:layout_constraintStart_toStartOf="@+id/ivVariant"
        app:layout_constraintTop_toBottomOf="@+id/ivVariant"
        tools:text="Variant Title" />

</androidx.constraintlayout.widget.ConstraintLayout>